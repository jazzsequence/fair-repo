{
    "name": "jazzsequence/fair-repo",
    "description": "jazzsequence FAIR repo",
    "type": "project",
    "license": "MIT",
    "authors": [
        {
            "name": "Chris Reynolds",
            "email": "chris@jazzsequence.com"
        }
    ],
    "repositories": [
        {
            "type": "composer",
            "url": "https://packages.jazzsequence.com"
        }
    ],
    "extra": {
        "installer-types": ["library"],
        "installer-paths": {
            "wp-content/mu-plugins/{$name}/": [
                "type:wordpress-muplugin",
                "fairpm/fair-plugin",
                "fair/mini-fair-repo"
            ],
            "wp-content/mu-plugins/singleton/": ["afragen/singleton"],
            "wp-content/plugins/{$name}/": ["type:wordpress-plugin"],
            "wp-content/themes/{$name}/": ["type:wordpress-theme"]
        }
    },    
    "minimum-stability": "dev",
    "prefer-stable": true,
    "require": {
        "afragen/singleton": "*",
        "composer/installers": "*",
        "fair/mini-fair-repo": "dev-main",
        "fairpm/fair-plugin": "*",
        "oomphinc/composer-installers-extender": "*"
    },
    "scripts": {
        "post-install-cmd": [
            "composer install --no-dev --prefer-dist --working-dir=./wp-content/mu-plugins/mini-fair-repo",
            "rm -rf wp-content/mu-plugins/mini-fair-repo/.git"
        ],
        "post-update-cmd": [
            "composer install --no-dev --prefer-dist --working-dir=./wp-content/mu-plugins/mini-fair-repo",
            "rm -rf wp-content/mu-plugins/mini-fair-repo/.git"
        ]
    },
    "config": {
        "optimize-autoloader": true,
        "preferred-install": "dist",
        "process-timeout": 0,
        "sort-packages": true,        
        "allow-plugins": {
            "composer/installers": true,
            "oomphinc/composer-installers-extender": true
        }
    }
}
